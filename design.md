# 股票持仓统计网页 — 设计文档

## 1. 技术选型

| 项 | 方案 | 理由 |
|----|------|------|
| 实现方式 | 单文件 `index.html`（内嵌 CSS + JS） | 无需构建、无需服务器、双击即用 |
| 语言 | 原生 HTML5 + CSS3 + ES6 | 无依赖，兼容现代浏览器 |
| 数据存储 | 浏览器 `localStorage` | 本地持久化，无后端 |
| 行情数据 | 腾讯行情接口 `qt.gtimg.cn` | 免费、无需 Key、支持跨域（`Access-Control-Allow-Origin: *`） |

## 2. 系统架构

单文件内部划分为三个层次：

```
┌──────────────────────────────────────┐
│  视图层（HTML + CSS）                  │
│  标题栏 / 汇总卡片 / 添加表单            │
│  持仓表格 / 关注表格（页签切换）          │
└──────────────────────────────────────┘
                 ▲
                 │ DOM 渲染、事件绑定
                 ▼
┌──────────────────────────────────────┐
│  逻辑层（JS）                          │
│  持仓增删改 / 关注增删 / 指标计算         │
│  自动获取 / 一键刷新 / 校验             │
└──────────────────────────────────────┘
                 ▲
                 │ 读写
                 ▼
┌──────────────────────────────────────┐
│  存储层（localStorage）                │
│  key: stock_holdings_v1（持仓）        │
│  key: stock_watchlist_v1（关注）       │
└──────────────────────────────────────┘
```

无后端，无第三方框架，所有逻辑在单个 JS 脚本内完成。

## 3. 数据模型

### 3.1 localStorage 结构

```json
{
  "stock_holdings_v1": [
    {
      "id": "uuid",
      "code": "600519",
      "name": "贵州茅台",
      "quantity": 100,
      "costPrice": 1500.00,
      "currentPrice": 1356.31,
      "prevClose": 1343.00,
      "dividendYield": 3.84,
      "updatedAt": "2026-08-13T12:00:00Z"
    }
  ],
  "stock_watchlist_v1": [
    {
      "id": "uuid",
      "code": "300750",
      "name": "宁德时代",
      "currentPrice": 186.50,
      "prevClose": 185.20,
      "dividendYield": 1.20,
      "updatedAt": "2026-08-13T12:00:00Z"
    }
  ]
}
```

- `dividendYield` 存储百分比数值（`3.84` = 3.84%），计算时除以 100。
- `id` 使用 `Date.now().toString(36) + 随机数` 生成，保证唯一。
- 持仓与关注分别存储于两个 key，互不干扰；关注记录不含 `quantity`、`costPrice` 字段。
- `prevClose` 为昨收价，用于计算当日盈亏与涨跌幅，由行情接口自动获取。

### 3.2 派生指标（不存储，渲染时计算）

```
市值       = quantity × currentPrice
成本       = quantity × costPrice
盈亏       = 市值 − 成本
盈亏率     = 盈亏 / 成本 × 100%
年分红     = 市值 × dividendYield / 100
当日盈亏   = (currentPrice − prevClose) × quantity
当日盈亏率 = (currentPrice − prevClose) / prevClose × 100%
每股股息   = currentPrice × dividendYield / 100
目标价(y)  = 每股股息 ÷ y × 100 = currentPrice × dividendYield ÷ y
```

其中 `y` 为目标股息率档位（5.0 / 5.5 / … / 8.5）。

## 4. 界面设计

### 4.1 配色规范

| 用途 | 色值 | 说明 |
|------|------|------|
| 主色 | `#2563eb` | 按钮、链接、强调 |
| 深主色 | `#1e40af` | 标题栏、悬停态 |
| 浅蓝背景 | `#eff6ff` | 卡片、表头、输入框焦点 |
| 页面背景 | `#f0f7ff` | 全局背景 |
| 卡片背景 | `#ffffff` | 内容容器 |
| 主文字 | `#1e293b` | 标题、正文 |
| 次要文字 | `#64748b` | 标签、辅助说明 |
| 盈利 | `#dc2626` | 正盈亏（A 股惯例红涨） |
| 亏损 | `#16a34a` | 负盈亏（A 股惯例绿跌） |
| 边框 | `#dbeafe` | 分隔线、输入框描边 |
| 表格斑马纹 | `#eef4fc` | 列表偶数行底纹（略深于纯白，增强行分隔辨识） |
| 行悬停 | `#eaf2ff` | 鼠标悬停行高亮 |

### 4.2 布局结构

```
┌────────────────────────────────────────┐
│ 标题栏（深蓝渐变 #1e40af → #2563eb）      │
│   🏦 股票持仓统计   [自动刷新:开/关]         │
│            [刷新行情] [导出] [导入]        │
├────────────────────────────────────────┤
│ 页签： [持仓] [关注]（切换内容区）         │
├────────────────────────────────────────┤
│ 汇总卡片区（仅「持仓」页显示，6 张白色卡片）│
│  总市值 │ 总成本 │ 总盈亏 │ 当日盈亏 │       │
│      预计年分红 │ 加权平均股息率             │
├────────────────────────────────────────┤
│ 添加表单（随页签切换，卡片标题旁带折叠按钮）│
│  持仓页：[代码][查询] 名称/现价/股息率      │
│         [数量][成本价]        [添加持仓]  │
│  关注页：[代码][查询] 名称/现价/股息率      │
│                           [添加关注]     │
├────────────────────────────────────────┤
│ 列表（随页签切换）                        │
│  持仓：[搜索][清空]                        │
│        代码 名称 数量 成本价 现价 股息率    │
│        市值 成本 盈亏 盈亏率 当日盈亏 占比   │
│        年分红  [操作：编辑/删除]           │
│  关注：[搜索][清空]                        │
│        代码 名称 现价 股息率 涨跌幅        │
│        [操作：转为持仓/删除]              │
├────────────────────────────────────────┤
│ 股息率目标价对照表（白色卡片）             │
│  覆盖持仓 + 关注全部股票，可折叠            │
│  股票     5%   5.5% 6%  6.5% 7% 7.5% 8% 8.5%│
│  贵州茅台 1041 947  868 800 744 694 651 613 │
│  ...                                      │
└────────────────────────────────────────┘
```

- 对照表列头为 8 档目标股息率（`5.0% ~ 8.5%`，步长 0.5%），行首为股票名称。
- 单元格为「达到该股息率所需的价格」，价格低于现价时提示需回撤的空间，现价已满足该档时高亮标绿。

- 盈亏、盈亏率、当日盈亏、年分红按正负着色（盈红亏绿）。
- 汇总卡片分红金额突出显示（主色加粗）。
- 关注页不展示汇总卡片，只关注行情本身。
- 关注列表额外展示「涨跌幅」，便于观察异动。

### 4.3 交互设计

| 交互 | 行为 |
|------|------|
| 输入代码后点「查询」 | 请求行情接口，自动填充名称、当前价、股息率；失败则提示可手动填写 |
| 点「添加持仓」 | 校验字段 → 写入持仓 → 重渲染 → 清空表单并聚焦回代码框 |
| 点「添加关注」 | 校验字段 → 写入关注列表 → 重渲染 → 清空表单并聚焦回代码框 |
| 代码框内按回车 | 等同于点击「查询」或「添加」（按当前页签与输入状态智能触发） |
| 点「持仓 / 关注」页签 | 切换内容区，展示对应列表与表单 |
| 点添加表单折叠按钮 | 收起/展开添加表单，折叠状态持久化到 localStorage |
| 点「删除」（持仓） | 弹确认框 → 移除持仓记录 → 重渲染 |
| 点「编辑」 | 行内进入编辑态 → 保存后重渲染 |
| 点「删除」（关注） | 弹确认框 → 移除关注记录 → 重渲染 |
| 点「转为持仓」 | 将该关注股票填入持仓表单（预填名称/当前价/股息率）并切到持仓页 |
| 点「刷新行情」 | 遍历持仓与关注请求行情，更新当前价、昨收价与股息率，显示进度/失败提示 |
| 切换「自动刷新」开关 | 开启后每 5 秒静默刷新一次；关闭则停止定时刷新 |
| 点表头 | 切换该列升序/降序排列（持仓默认按市值降序） |
| 输入搜索框 | 按名称或代码实时过滤当前页签的表格行 |
| 点「清空全部」 | 弹确认框 → 删除当前页签下全部记录 |
| 点「导出」 | 生成 JSON 文件下载（同时含持仓与关注） |
| 点「导入」 | 选择 JSON 文件 → 校验 → 合并/覆盖持仓与关注 |
| 点目标价表折叠按钮 | 收起/展开目标价对照表，折叠状态持久化 |
| 目标价表自动更新 | 随行情刷新（含每 5 秒自动刷新）同步重算，无需手动操作 |

## 5. 核心逻辑设计

### 5.1 自动获取行情

**接口**：`https://qt.gtimg.cn/q={市场前缀}{代码}`

**市场前缀规则**：
- `6 / 9 / 5` 开头 → `sh`
- `0 / 3 / 2 / 1` 开头 → `sz`
- `8 / 4` 开头 → `bj`

**响应**：GBK 编码文本，形如
```
v_sh600519="1~贵州茅台~600519~1356.31~...~3.84~...";
```

**处理流程**：
```js
const res = await fetch(`https://qt.gtimg.cn/q=${prefix}${code}`);
const buf = await res.arrayBuffer();
const text = new TextDecoder('gbk').decode(buf);   // 解决 GBK 乱码
const fields = text.split('~');
name          = fields[1];    // 股票名称
currentPrice  = fields[3];    // 当前价
prevClose     = fields[4];    // 昨收价
changePercent = fields[32];   // 涨跌幅（%）
dividendYield = fields[64];   // 股息率（%）
```

字段索引已实测验证（贵州茅台 3.84%、工商银行 4.12%、长江电力 3.57%、五粮液 6.82%）。

### 5.2 添加/编辑校验规则

- 代码：6 位数字
- 数量：正整数
- 成本价：数字，可为负数（如分红摊薄后成本为负）
- 当前价：非负数字
- 股息率：`0 ~ 100` 的数字
- 校验失败在对应输入框下方提示，不执行写入

### 5.3 渲染流程

```
数据变更（增/删/改/导入）
        │
        ▼
更新 localStorage
        │
        ▼
重新计算所有派生指标
        │
        ▼
重绘汇总卡片 + 持仓表格
```

### 5.4 一键刷新行情

```
点击「刷新行情」
        │
        ▼
遍历持仓列表 + 关注列表，逐条 fetch 行情接口
        │
        ▼
更新各记录 currentPrice / prevClose / dividendYield
        │
        ▼
全部完成后重渲染，提示成功/失败条数
```

- 顺序请求避免并发压力；单条失败跳过并计入失败数。
- 刷新期间按钮禁用，显示加载状态。
- 持仓与关注共用同一刷新按钮，一次性更新两处数据。

### 5.5 排序与筛选

- 排序：点击表头切换该列升/降序。
  - 持仓列：市值、成本、盈亏、盈亏率、当日盈亏、股息率、年分红。
  - 关注列：当前价、股息率、涨跌幅。
- 筛选：搜索框输入时，按名称（包含匹配）或代码（前缀匹配）过滤渲染，不影响底层数据。

### 5.6 关注列表逻辑

- 关注记录仅存代码、名称、当前价、股息率，展示时额外呈现涨跌幅（由行情接口即时返回，或与当前价一并存储）。
- 添加关注时校验同一列表内代码唯一：重复代码提示“该股票已在关注列表中”。
- 「转为持仓」：点击后将该关注股票的代码、名称、当前价、股息率预填入持仓表单，并切换至持仓页签，由用户补充数量与成本价后提交；转成功后是否自动删除关注记录由实现决定（建议提示确认）。

### 5.7 数字格式化

- 金额与盈亏：`Intl.NumberFormat('zh-CN')` 千分位，保留两位小数。
- 股息率、盈亏率、占比、涨跌幅：保留两位小数并附加 `%`。
- 盈亏按正负着色：正 `#dc2626`（红），负 `#16a34a`（绿）；涨跌幅同色规则。

### 5.8 自动刷新（5 秒轮询）

```
定时器（每 5 秒）→ 静默刷新全部行情
        │
        ▼
更新 currentPrice / prevClose / dividendYield
        │
        ▼
重渲染（不弹提示、不打断操作）
```

- **开关**：标题栏「自动刷新」开关，状态持久化到 localStorage，默认开启。
- **静默**：自动刷新不弹 toast、不改变按钮文案、不打断手动查询与表单输入。
- **并发保护**：上一轮请求未完成时不启动下一轮（in-flight 标志位），避免请求堆积。
- **编辑保护**：存在正在编辑的行时，本轮跳过该行数据更新，保存/取消后再恢复刷新。
- **页面隐藏**：`document.visibilitychange` 隐藏时暂停定时器，回到页面立即刷新一次，节省接口请求。
- **失败处理**：单条失败静默跳过，保留上一份数据，不提示。
- **频率说明**：免费接口按「持仓 + 关注」总数逐条请求，5 秒间隔在常见规模（数十只）下可接受；数量较多时可加大间隔或改用合并请求。

### 5.9 添加面板折叠

- 添加表单卡片标题右侧放置折叠按钮（chevron `▾ / ▸`），点击切换表单区域的显隐。
- 折叠状态写入 localStorage（key 如 `form_collapsed`），刷新后保持用户上次的选择。
- 折叠仅隐藏表单内容区，不影响页签切换、列表与汇总展示。
- 展开/收起使用 `max-height` 过渡动画，避免突兀跳动。

### 5.10 股息率目标价

- **数据来源**：遍历持仓 + 关注，取每只股票的 `currentPrice` 与 `dividendYield`。
- **每股股息** = `currentPrice × dividendYield / 100`。
- **档位**：`y ∈ {5.0, 5.5, 6.0, 6.5, 7.0, 7.5, 8.0, 8.5}`，目标价 = `currentPrice × dividendYield / y`。
- **展示**：矩阵表，行 = 股票（按当前股息率降序），列 = 8 档目标股息率，单元格为对应价格（`fmtMoney` 千分位）。
- **着色提示**：
  - 目标价 ≤ 现价：说明需股价回撤才能达到该档，附回撤幅度（`1 − 目标价/现价`）。
  - 目标价 ≥ 现价：说明现价下已满足该档股息率，高亮标绿。
- **边界**：`dividendYield` 缺失或为 0 的股票，该行显示“无股息数据”，不参与计算。
- **实时性**：目标价随行情刷新（手动或每 5 秒自动）同步重算。

## 6. 错误处理

| 场景 | 处理 |
|------|------|
| 网络失败 / 接口超时 | 「查询」提示“获取失败，请手动填写”，不阻塞添加 |
| 未知股票代码 | 接口返回空数据 → 提示“未找到该股票” |
| 重复添加 | 同一列表内代码重复时提示“该股票已存在”，不写入 |
| 接口限流 / 频率过高 | 自动刷新失败静默跳过；连续失败时自动延长轮询间隔并提示 |
| localStorage 损坏 | 启动时 try/catch，异常则重置为空列表 |
| 非法输入 | 前端校验拦截，给出中文提示 |

## 7. 目录结构

```
Interest/
├── index.html       # 主文件（全部功能）
├── 需求文档.md
└── 设计文档.md
```

## 8. 后续可扩展方向

- 支持港股 / 美股行情源
- 分红记录（按季度/年度录入，精确计算实际分红）
- 多组合 / 多账户管理
- 持仓成本摊薄计算
- 图表可视化（持仓占比、盈亏趋势）
- 关注列表到价提醒（价格 / 涨跌幅触发通知）
